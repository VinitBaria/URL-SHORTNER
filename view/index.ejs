<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
</head>
<body>
    <h1>Welcome to URL Shortener !!!</h1>
<form action="/" method="POST">
        <label for="orgurl">Enter URL to shorten:</label>
        <input type="url" id="orgurl" name="orgurl" required>
        <button type="submit">Shorten</button>
</form>
    <h2>Your Shortened URL:</h2>
<% array.forEach(url => { %>

            <li>
                Original URL: 
                <a href="<%= url.orgurl %>"><%= url.orgurl %></a><br>

                Shortened URL: 
                <a href="<%= url.shortId %>"><%= url.shortId %></a>
                
              <a href="<%= url.shortId %>/stats">click here</a>
                ️ to see stats
            </li>
            <br>
        <% }); %>
    

</body>
</html>
